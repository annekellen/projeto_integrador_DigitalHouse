import React, { useState } from "react";
import "./style.css";
import FormSignup from "../Avaliacao/FormSignUp"
import FormSuccess from "../Avaliacao/FormSucces";

const Form = () => {
  const [isSubmitted, setIsSubmitted] = useState(false);

  function submitForm() {
    setIsSubmitted(true);
  }
  return (
    <>
      <div className="form-container">
        {!isSubmitted ? (
          <FormSignup submitForm={submitForm} />
        ) : (
          <FormSuccess />
        )}
      </div>
    </>
  );
};

export default Form;